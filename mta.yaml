_schema-version: "3.2"
ID: prueba
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: prueba-destination-content
  type: com.sap.application.content
  requires:
  - name: prueba-destination-service
    parameters:
      content-target: true
  - name: prueba-repo-host
    parameters:
      service-key:
        name: prueba-repo-host-key
  - name: prueba-uaa
    parameters:
      service-key:
        name: prueba-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: prueba_html_repo_host
          ServiceInstanceName: prueba-html5-service
          ServiceKeyName: prueba-repo-host-key
          sap.cloud.service: prueba
        - Authentication: OAuth2UserTokenExchange
          Name: prueba_uaa
          ServiceInstanceName: prueba-xsuaa-service
          ServiceKeyName: prueba-uaa-key
          sap.cloud.service: prueba
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: prueba-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: prueba-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - prueba.zip
      name: prueba
      target-path: resources/
- name: prueba
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: prueba-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: prueba-destination-service
    service-plan: lite
- name: prueba-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: prueba-xsuaa-service
    service-plan: application
- name: prueba-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: prueba-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
